[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests requiring LLM API calls (skip by default in CI)
    integration: tests requiring runtime environment (dashboard, agent loop)
addopts = -v --tb=short
